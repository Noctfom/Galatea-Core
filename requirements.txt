# ========================================================
# Galatea-Core v3.5.0 Dependencies
# ========================================================
# Install: pip install -r requirements.txt
# PyTorch must be installed for the target CPU/CUDA runtime before this file.
# ========================================================

# -- Core ML Framework --
# Install PyTorch separately based on your CUDA version:
# Current Windows bundle: pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cu130
# CPU Only: pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
# 框架不导入 torchvision/torchaudio，避免它们因版本联动替换已配置好的 Torch。

# -- WebUI --
streamlit>=1.28.0

# -- Training Monitoring --
tensorboard>=2.14.0

# -- Math / Data Processing --
numpy>=1.24.0
pandas>=2.0.0
sentence_transformers

# -- Inter-Process Communication --
pyzmq>=25.0.0

# -- ONNX Inference Acceleration (optional, for --use_onnx) --
onnxruntime>=1.16.0
onnxscript
onnx

# -- System Monitoring --
psutil>=5.9.0

# -- CLI Rich Output --
rich>=13.0.0

# -- Online Deck Fetching --
requests>=2.31.0

# -- Environment Validation --
packaging>=23.1
